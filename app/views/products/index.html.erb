<% @products.each do |product| %>
<div class="col-sm-3">
    <%= link_to(image_tag(product.image.thumb.url),product_path(product)) %>
    <h6><%= product.title %></h6>
    <p><%= product.price %></p>

</div>
<% end %>
